{
  "name": "accounting-standards-site",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "sync:docs": "bun run scripts/sync-docs.ts",
    "dev": "bun run sync:docs && astro dev",
    "start": "bun run sync:docs && astro dev",
    "build": "bun run sync:docs && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "postbuild": "pagefind --site dist"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "tailwindcss": "^4.0.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "pagefind": "^1.3.0",
    "typescript": "^5.3.0"
  }
}
